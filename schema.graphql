type Query {
    hello: String
    posts: [Post!]
    # comments: [Comment!]
  }

  type Post {
    id: ID!   
    content: String!
    author: User!             
    # comments: [Comment!]
  }

    type User {
    id: ID!
    nickname: String!
    image: String
    posts: [Post!]!
  }

  # type Comment{
  #   id: ID!
  #   content: String!
  #   author: User!
  #   post: Post!
  # }